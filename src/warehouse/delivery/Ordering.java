package warehouse.delivery;

import warehouse.user.UserRegister;

import java.util.ArrayList;

public class Ordering {
    static ArrayList<String> orderInfo = new ArrayList<>();


    public void ordering(UserRegister user, DeliveryRegister delivery) {
//        orderInfo.add(Integer.parseInt(user.toString()),delivery.toString());
//        return orderInfo.add(user.toString() + delivery.toString() + "\n");
        System.out.println("sfsgsdg");
    }
}
